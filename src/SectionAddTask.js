export default function addTaskSection()
{
    return (
        <section className="add-task-section">
            <input type="text" placeholder="اضف المهمه هنا" />
            <button class="btn-add-task">اضف</button>
        </section>
    )
}